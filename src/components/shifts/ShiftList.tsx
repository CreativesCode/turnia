/**
 * Lista de turnos con filtros: equipo, tipo, usuario, fecha.
 * @see indications.md §5.2
 */
export function ShiftList() {
  return (
    <div className="rounded-lg border border-border bg-background p-4">
      <p className="text-muted">Lista de turnos (filtros: equipo, tipo, usuario, fecha)</p>
    </div>
  );
}
